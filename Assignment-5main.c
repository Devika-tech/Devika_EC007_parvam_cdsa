#include <stdio.h>
void main()
{
    int age,monthly income;
    scanf("%d",&age);
    scanf("%d",&monthly income);
     if (18 <= age <= 25)
        if monthly income >= 5000:
            printf("Eligible");
    elif (26 <= age <= 40)
        if monthly income >= 10000:
            printf("Eligible");
    elif (41 <= age <= 60)
        if monthly_income >= 15000:
           printf("Eligible");
    else
        printf("Not Eligible");

}
